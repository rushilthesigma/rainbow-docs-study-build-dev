node_modules/
dist/
.env
.env.mobile.local
android/key.properties
android/app/*.jks
android/app/*.keystore
.claude/
uploads/
users.json
sessions.json
# Email-at-rest encryption key — server-only secret, NEVER commit.
.email-enc-key
social.json
textbooks.json
parties.json
*.log
.DS_Store

# Stashed Figma-style workspace revamp (restorable if we ever want it back)
.figma-revamp/

.write-test

# Backup files are not source.
*.bak
